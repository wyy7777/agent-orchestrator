## 架构

```
┌─────────────────────────────────────────┐
│          前端 (Next.js 静态文件)          │
│   Dashboard │ 工作流 │ 任务 │ 审批       │
└────────────────────┬────────────────────┘
                     │ HTTP / WebSocket
┌────────────────────┴────────────────────┐
│         FastAPI 后端 (单进程)             │
│                                         │
│  REST API ── WebSocket ── 状态机引擎     │
│       │                     │           │
│       └── 步骤处理器 ───────┘           │
│           (analyze/execute/review)       │
│                  │                       │
│           AI 代理层                       │
│       DeepSeek / OpenAI / Claude         │
│                  │                       │
│           ┌──────┴──────┐               │
│           │ GitHub 集成  │               │
│           └──────┬──────┘               │
│                  │                       │
│                 GitHub API               │
│           (Webhook / 代码同步)            │
│                  │                       │
│           SQLite 数据库                   │
└─────────────────────────────────────────┘
```